#配置项目启动端口号
server:
  port: 8217
#配置数据源
spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 100MB
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: Zzt030208
    url: jdbc:mysql://localhost:8326/yuan_zhou?useUnicode=true&characterEncoding=UTF-8&zeroDateTimeBehavior=convertToNull&serverTimezone=GMT%2B8
mybatis-plus:
  global-config:
    db-config:
      id-type: auto #数据库id自增
  configuration:
    map-underscore-to-camel-case: false  # 开启驼峰命名
    auto-mapping-behavior: full # 自动映射任何复杂结果
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:/mapper/*.xml

backend:
   path: D:\yuan_zhouData\


